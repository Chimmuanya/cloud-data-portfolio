#!/usr/bin/env bash
# project-07-hospital-queue-simulator/scripts/deploy.sh - project deploy helper (template)
# Usage: bash scripts/deploy.sh
echo "This is a placeholder deploy helper for project-07-hospital-queue-simulator. Replace with real deploy commands."
echo "Example: sam build && sam deploy --guided --profile deploy-ci --region eu-west-1"

