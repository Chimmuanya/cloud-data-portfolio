#!/usr/bin/env bash
# project-08-invoice-idp/scripts/deploy.sh - project deploy helper (template)
# Usage: bash scripts/deploy.sh
echo "This is a placeholder deploy helper for project-08-invoice-idp. Replace with real deploy commands."
echo "Example: sam build && sam deploy --guided --profile deploy-ci --region eu-west-1"

